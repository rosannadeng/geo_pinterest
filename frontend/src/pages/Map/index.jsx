import React from 'react';
import { Layout } from 'antd';

const { Content } = Layout;

const MapPage = () => {
    return (
        <Layout>
            <Content>
                <h1>Map Page</h1>
            </Content>
        </Layout>
    );
};

export default MapPage;